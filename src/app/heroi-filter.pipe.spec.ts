import { HeroiFilterPipe } from './heroi-filter.pipe';

describe('HeroiFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroiFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
